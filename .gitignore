# Dependencies
/tehillim-campaign-manager/vendor/
node_modules/

# Tool caches
.phpcs.cache
.phpunit.result.cache
**/.phpunit.result.cache

# Build artifacts (distribution ZIPs are generated, not committed)
/*.zip

# OS / editor
.DS_Store
*.log
